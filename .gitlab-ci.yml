image: ruby:2.3

before_script:
  - whoami
  - ruby -v
  - which ruby
  - bundle install

build:
  stage: build
  script:
    - echo "This is the build stage"

test:
  stage: test
  script:
    - echo "This is test stage"
  cache:
    paths:
      - vendor/bundle

staging:
  stage: deploy
  script:
    - echo "This is the staging stage"
    - cap staging deploy
  cache:
    paths:
      - vendor/bundle
  only:
    - stage